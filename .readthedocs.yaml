version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
    nodejs: "20"

  jobs:
    post_install:
      - npm ci

    build:
      html:
        - npm run docs
        - mkdir -p $READTHEDOCS_OUTPUT/html
        - cp -r docs/* $READTHEDOCS_OUTPUT/html/
